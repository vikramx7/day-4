function DefaultProps(props)
{
    return(
        <div>
        <h1>DefaultProps value is {props.name}</h1>
        </div>
    );
}
export default DefaultProps;
DefaultProps.defaultProps={
    name:"Sanjaaa"
}